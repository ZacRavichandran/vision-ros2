

# detector_node parameters
detector_node:
  ros__parameters:
    weights:               /home/dcist/data/weights/sam3.pt
    confidence:            0.5
    labels:                "desk,chair"
    camera_frame:          prometheus/frontright
    tracker_n_dets:        5
    track_distance_thresh: 5.0
    scale_depth:           true
    detection_depth_scale: 1000.0  # realsense depth is in mm
    flip_img:              false
    color_sub_topic:    /prometheus/frontright/color/image_raw
    depth_sub_topic:  /prometheus/frontright/depth/image_rect
    depth_info_sub_topic:  /prometheus/frontright/color/camera_info
    odom_sub_topic:         spot/odom
    camera_to_body_rotation: [ 0.67956665, -0.47025164, 0.20790072, -0.52328754]



# vlm_node parameters
vlm_node:
  ros__parameters:
    flip_img: false
    color_sub_topic:    /prometheus/frontright/color/image_raw
